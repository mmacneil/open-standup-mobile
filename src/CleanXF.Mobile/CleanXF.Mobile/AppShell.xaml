<?xml version="1.0" encoding="UTF-8"?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms" 
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:d="http://xamarin.com/schemas/2014/forms/design"
       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
       mc:Ignorable="d"
       xmlns:local="clr-namespace:CleanXF.Mobile.Views" 
       xmlns:controls="clr-namespace:CleanXF.Mobile.Controls"
       Title="CleanXF.Mobile"
       x:Class="CleanXF.Mobile.AppShell">

    <!-- 
        Styles and Resources 
    -->
    <Shell.Resources>        
        <ResourceDictionary Source="ShellStyles.xaml"/>
    </Shell.Resources>

    <Shell.FlyoutHeader>
        <controls:FlyoutHeader />
    </Shell.FlyoutHeader>

    <!-- Loading/Start Page -->
    <ShellItem Route="initialize">
        <ShellContent ContentTemplate="{DataTemplate local:InitializePage}" />
    </ShellItem>

    <FlyoutItem Title="Animals"
                FlyoutDisplayOptions="AsMultipleItems">
        
        <ShellContent Title="Monkeys"
                      Icon="tab_feed.png" ContentTemplate="{DataTemplate local:ItemsPage}">           
        </ShellContent>
        <ShellContent Title="Elephants"
                      Icon="tab_feed.png">
            <local:ItemsPage />
        </ShellContent>

    </FlyoutItem>


    <MenuItem Text="Sign out"
              IconImageSource="tab_feed.png"
              Command="{Binding SignoutCommand}" />

</Shell>
